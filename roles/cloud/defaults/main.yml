---
# Cloud role defaults

# Cloud provider configuration
cloud_provider: "openstack"  # openstack, aws, azure
cloud_region: "regionOne"

# Credentials paths
cloud_credentials_path: "/etc/rift/cloud/credentials"
cloud_config_path: "/etc/rift/cloud/config" 